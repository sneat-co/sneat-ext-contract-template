{
  "$schema": "./node_modules/nx/schemas/nx-schema.json",
  "namedInputs": {
    "default": [
      "{projectRoot}/**/*",
      "sharedGlobals"
    ],
    "production": [
      "default",
      "!{projectRoot}/eslint.config.mjs",
      "!{projectRoot}/**/?(*.)+(spec|test).[jt]s?(x)?(.snap)",
      "!{projectRoot}/tsconfig.spec.json",
      "!{projectRoot}/src/test-setup.[jt]s",
      "!{projectRoot}/test-setup.[jt]s"
    ],
    "sharedGlobals": []
  },
  "targetDefaults": {
    "@nx/angular:package": {
      "cache": true,
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        "production",
        "^production"
      ]
    },
    "@nx/eslint:lint": {
      "cache": true,
      "inputs": [
        "default",
        "^default",
        "{workspaceRoot}/eslint.config.mjs"
      ]
    },
    "@nx/vitest:test": {
      "cache": true,
      "inputs": [
        "default",
        "^production"
      ]
    }
  },
  "release": {
    "projects": [
      "ext-template-contract"
    ],
    "projectsRelationship": "fixed",
    "version": {
      "// conventionalCommits": "Load-bearing in CI, and its absence fails silently. Without it `nx release --yes` has no way to compute a bump, so it drops into its interactive version picker ('Custom exact version'), reads no input on a non-TTY runner, publishes nothing — and still exits 0. The workflow goes green while npm stays empty. Mirrors ext-debtus.",
      "conventionalCommits": true,
      "// fallbackCurrentVersionResolver": "currentVersionResolver is git-tag (see the project's project.json); a repo with no tags yet has nothing to read, so fall back to the version on disk.",
      "fallbackCurrentVersionResolver": "disk",
      "preVersionCommand": "pnpm exec nx run ext-template-contract:build",
      "versionActions": "@nx/js/src/release/version-actions"
    },
    "changelog": {
      "automaticFromRef": true
    },
    "releaseTag": {
      "pattern": "v{version}"
    }
  },
  "plugins": [
    {
      "plugin": "@nx/eslint/plugin",
      "options": {
        "targetName": "lint"
      }
    },
    {
      "plugin": "@nx/vite/plugin",
      "options": {
        "buildTargetName": "build",
        "testTargetName": "test"
      }
    },
    {
      "plugin": "@nx/vitest",
      "options": {
        "testTargetName": "vite:test"
      }
    }
  ],
  "generators": {
    "@nx/angular:library": {
      "linter": "eslint",
      "unitTestRunner": "vitest-analog"
    }
  },
  "analytics": false
}
